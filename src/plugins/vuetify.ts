import 'vuetify/styles'
import { createVuetify } from 'vuetify'
import { VApp, VList, VNavigationDrawer, VToolbar } from 'vuetify/components'

export default createVuetify({
    components: {
        VApp,
        VNavigationDrawer,
        VList,
        VToolbar,
    },
})
